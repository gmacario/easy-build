version: '1.0'
stages: []
steps:
  BuildingDockerImage:
    title: Building Docker Image
    type: build
    image_name: gmacario/easybuild
    working_directory: ./
    tag: '${{CF_BRANCH_TAG_NORMALIZED}}'
    dockerfile: Dockerfile

# EOF
